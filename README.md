Get the [Haskell Platform](www.haskell.org/platform/) and install the dependencies for 2048:

`cabal install monad-loops split`

Finally, run it with `runhaskell Main.hs`

Controls are i, j, k, l for Up, Left, Down, Right.
